这是消毒柜公司成品的真实 Calibration 拆解。必须严格执行以下规则：

1. 先识别真实产品主体。消毒柜整机、柜体或明确的产品实物必须输出为 product_image；不得因为产品与背景同色、位于画面边缘或被文字覆盖而遗漏。
2. main_title 只框主标题本身，subtitle、selling_point、body_text 分别独立。文字框不得覆盖整张画布，也不得用一个大框替代多个视觉上独立的文字组。
3. layout_block 只表示确实可见、边界明确的版面容器。禁止为每个已识别内容模块再复制一个同尺寸 layout_block。
4. 父容器可以包含子模块；兄弟模块之间不得大面积重叠。若无法确认父子关系，优先输出更具体的内容模块并降低 confidence。
5. 所有 x、y、width、height 都是相对原图的 0 到 1 归一化坐标。x + width 与 y + height 不得超过 1。
6. blueprint_modules 不得重复；相同 type 且高度重合的框只能保留一个。
7. 只输出图中实际存在的模块。不要根据常见电商模板虚构 Logo、按钮、装饰或卖点。
8. 输出前逐项自检：至少检查产品是否遗漏、主标题是否遗漏、坐标是否越界、重复框、兄弟框无效重叠。
