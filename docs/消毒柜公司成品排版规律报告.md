# 消毒柜公司成品排版规律报告

## 当前结论

本批已扫描 56 张 PNG，全部为竖版。解析器从人工彩色描边中提出 343 个候选区域：

- 红色 `layout_block`：110
- 蓝色 `product_image`：125
- 绿色 `main_text`：108

当前本地公司成品目录已有 56 张无彩框候选原图。数据库现有 30 条已人工确认、
已保存原图配对的标注，另有 26 条待人工配对与审核。30 条 verified 标注按项目组
分为 24 条 Calibration 和 6 条密封 Holdout。此数据准备度不等于模型质量通过，
也不能据此宣称系统已经学会排版规律。

## 证据边界

- 只有 `source_type=company_published` 且经人工 `verified` 的标注才进入规律统计。
- `pending_review`、外部参考、被拒方案和模型自身预测均不作为正式规律证据。
- 导入和颜色解析不会自动验证蓝图，不会设置黄金项目。
- holdout 数据不得进入 few-shot 示例。

## 扫描质量

- 格式：PNG 56 张。
- 方向：portrait 56 张。
- 常见尺寸：957×1280（28 张）、960×1280（8 张）、959×1280（5 张），另有少量相近尺寸。
- 已保存且文件存在的精确原图配对：30；待人工确认候选：26；缺少候选原图：0。
- 56 条标注路径已迁移到当前 `消毒柜` 目录，30 条已配对原图路径均可读取。
- 原图候选无 SHA-256 重复组；存在 22 组同名不同扩展名冲突，需人工辨别。
- 15 条当前配对或候选的像素尺寸与彩框图不一致，不能仅凭文件名强行配对。
- 红框候选边重复警告 27 次、重叠框警告 17 次、断裂/缺失红框警告 6 次、断裂/缺失绿框警告 5 次、过小绿框警告 2 次、绿色重复边警告 1 次。
- 35 张图存在除“缺少无框原图”之外的解析歧义，必须优先人工检查。完整文件名及逐条 warning 已写入机器报告的 `ambiguous_items`。

## 当前可报告的业务规律

目前 verified 数量为 30，已达到该品类的数据集准备门槛；但模型 Canary 的主文字
识别率和布局模块召回率仍未达标，所以当前只可继续人工核验与 Calibration 校正。
系统仅使用 verified 的 `company_published` 标注计算：

- 画布比例、方向与平均模块数。
- 产品框和主文字框的平均位置及面积占比。
- 产品与文字的上下、左右关系。
- 外边距、列聚类、页面角色分布和缺少关键框的异常案例。

在这些统计生成前，不使用“高级感”“好看”“爆款”等不可验证结论。

## 下一步人工动作

1. 打开 `/annotation-learning`，默认显示 26 条待审核标注。
2. 对照左侧标注图与右侧结构框架，修正漏框、错框和边界。
3. 填写项目归属、页面角色和审核人。
4. 先人工确认候选无框原图，再由设计负责人逐张确认标注。
5. 已有 24/6 独立划分；不得读取 Holdout，先让 3 张 Canary 达到业务门槛。

验证前必须填写 `project_key`。系统以整个项目/页面组为单位分配约 80% calibration 和 20% holdout，不会把同一项目拆入两组。样本、项目组或无框原图不足时，评估状态保持 `not_ready`。

## 自动拆解与评估门槛

- few-shot 只读取 `company_published + verified + calibration`，最多 5 张；外部参考、被拒方案、待审核标注和 holdout 均被排除。
- 模型失败、无产品主体、非法坐标或严重重叠时进入人工复核；不生成伪造蓝图。
- AI 初始蓝图、引用标注 ID、模型名、Prompt 版本、生成方式和人工最终蓝图分别保存。
- holdout 指标包括产品框 IoU、主文字框 IoU、排版块平均 IoU、类型准确率、漏检、多检、越界、人工修改次数和无需修改率。
- 工程门槛为产品识别准确率 ≥90%、主文字识别准确率 ≥80%、模块类型准确率 ≥80%、坐标合法率 100%。holdout 不用于反复调 Prompt。

机器可读的逐图扫描结果位于 `backend/evaluation/disinfection_cabinet_layout_report.json`。
