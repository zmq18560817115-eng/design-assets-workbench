# 当前阶段：P3.2-D 公司核心排版模式人工确认

当前唯一目标是将现有137张公司素材形成的16个 `candidate / unverified` 候选，精简为少量、清晰、可由设计负责人确认的公司排版模式。当前建议审核6个核心模式，另有1个需要人工判断；合并建议不会自动执行，拒绝或合并记录只归档、不物理删除。

## 当前主线

```text
素材进入 → 结构拆解 → 排版模式 → 业务检索 → 人工反馈
```

当前停留在“排版模式”的人工确认门禁。完成后才进入真实业务检索验收。

## 正式模式门禁

- 至少3个不同公司案例；
- 代表案例明确；
- 模式名称已确认；
- 适用和不适用场景已确认；
- 必需模块和可选模块已确认；
- 设计负责人确认。

系统不得自动写入 `verified`。高度相似的模式只能建议合并；合并和拒绝均保留来源与审核历史。

## 本阶段暂停

- 视觉模型批量分析；
- Calibration 和 Holdout；
- 奶瓶导入；
- 模型微调；
- 候选设计审核智能体；
- 图片生成；
- 非主线页面优化。

暂停任务仅停止推进，不删除现有代码或历史数据。本阶段不调用视觉模型、不发送公司图片、不读取 Holdout、不执行 Calibration、不修改137张原始分析结果、不处理31张异常素材、不导入新素材、不删除数据库记录。

页面入口：`http://127.0.0.1:3100/patterns`。页面默认只展示待人工确认的核心候选，技术字段在“查看技术详情”中默认折叠。

阶段完成标志：`pattern_review_set_ready`。
